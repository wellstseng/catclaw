# _CHANGELOG.md

> 知識庫變更紀錄（最新在上，超過 8 筆觸發滾動淘汰）

| 日期 | 變更 | 影響文件 |
| 2026-04-06 | **docs: _AIDocs 審計 Session D — 覆蓋率驗證 + aidocs skills** — 新增 8 份模組文件（agent-system/subagent-system/exec-approval/mode/rate-limiter/session-snapshot/tool-log-store/inbound-history）。覆蓋率 modules/ 從 31→39 份。新增 `/aidocs-status`、`/aidocs-audit`、`/aidocs-update` 三個 skill（`src/skills/builtin/aidocs.ts`） | modules/*8(new), aidocs.ts(new), _INDEX.md, _CHANGELOG.md |
| 2026-04-06 | **refactor: 統一訊息管線 message-pipeline + Web Chat 升級** — 新增 `src/core/message-pipeline.ts`：`runMessagePipeline()` 統一 Memory Recall / Intent Detection / Prompt Assembly / Provider Trace / Inbound History / Session Memory / Context End 十步管線，所有平台共用。discord.ts 刪除 ~190 行重複管線碼改呼叫共用函式。dashboard.ts `/api/chat` 管線全開（與 Discord 一致），新增 session 選擇器下拉選單，支援跨平台 session 共用（選 Discord session → Web Chat 接續同一對話） | message-pipeline.ts(new), discord.ts, dashboard.ts |
| 2026-04-06 | **docs: _AIDocs 審計 Session C — 缺失模組文件新增** — 新增 6 份獨立模組文件：permission-gate（Tier + allow/deny 權限閘門）、ollama-provider（Ollama Native HTTP + NDJSON + think + tool_use 偵測）、vector-service（LanceDB in-process 向量服務 + namespace + graceful degradation）、task-store（per-session 任務 CRUD + 依賴管理）、task-ui（Discord ActionRow 按鈕互動）、mcp-client（MCP stdio JSON-RPC + 崩潰重連 + tool 自動註冊）。更新 message-trace.md 補齊 TraceContextStore + TracePromptBreakdown + 費用估算 + subagent 分類。C1/C3-C5/C12 因原始碼不存在或已有完整文件覆蓋而跳過 | modules/permission-gate.md(new), modules/ollama-provider.md(new), modules/vector-service.md(new), modules/task-store.md(new), modules/task-ui.md(new), modules/mcp-client.md(new), modules/message-trace.md(updated), _INDEX.md |
| 2026-04-06 | **feat: Context Snapshot 模組模式 + Dashboard mobile** — ①prompt-assembler 新增 AssembleSegment + extraBlockNames，trace 輸出含各段 name+content②message-trace TracePromptSegment，breakdown 加 segments(offset+length)③agent-loop 計算 segment offset 寫入 snapshot④discord.ts 傳入 extraBlockNames + assemblerSegments⑤Dashboard Context Snapshot [原始]/[模組] 雙模式切換，模組模式按來源分段可展開⑥Dashboard 新增 @media 響應式版面（≤768px/≤480px） | prompt-assembler.ts, message-trace.ts, agent-loop.ts, discord.ts, dashboard.ts |
| 2026-04-06 | **fix: _AIDocs 審計 M1-M10 中優先資訊補全** — ①workflow.md 補全 15+ 公開方法簽名（file-tracker/rut-detector/oscillation-detector/aidocs-manager/wisdom-engine/fix-escalation 等）②memory-engine.md 補 seedFromDir()+rebuildIndex()③02-CONFIG-REFERENCE.md 補 session/memory/workflow/accounts 設定區塊④tool-registry.md 補 web_fetch SSRF 保護+resultTokenCap+timeoutMs⑤hooks.md 補 reload()+runSessionStart/End() 簽名+SessionStart/EndInput 型別⑥safety.md 補 PermissionContext 介面+check() 內部分流表⑦cron.md 補 subagent 動作類型+欄位⑧skills.md 補 restart skill 中文 trigger⑨09-PITFALLS.md §18-21 編號順序修正⑩00-OVERVIEW.md config 欄位表補 session/memory/safety/workflow/accounts | modules/workflow.md, modules/memory-engine.md, 02-CONFIG-REFERENCE.md, modules/tool-registry.md, modules/hooks.md, modules/safety.md, modules/cron.md, modules/skills.md, 09-PITFALLS.md, 00-OVERVIEW.md |
| 2026-04-06 | **fix: _AIDocs 審計 H1-H7 高優先事實修正** — ①_INDEX.md skill 數量 21→28（25 TS + 3 prompt）②_INDEX.md modules/ 表補 5 文件（accounts/event-bus/hooks/safety/workflow）+ 頂層補 PLAN-V4③_INDEX.md ProviderType acp-cli→cli-claude/cli-gemini/cli-codex④memory-engine.md recall() 簽名修正（query/paths→prompt/ctx/overrides）⑤01-ARCHITECTURE.md 上傳路徑 catclaw-uploads→claude-discord-uploads⑥event-bus.md 補 cron:executed + skill:invoked 事件 | _INDEX.md, modules/memory-engine.md, 01-ARCHITECTURE.md, modules/event-bus.md |
| 2026-04-06 | **feat: TracePromptBreakdown 結構化分類** — TraceContextSnapshot 新增 promptBreakdown 欄位，分類記錄 system prompt 動態組裝來源（memoryContext/channelOverride/modeExtras/assemblerModules/agentLoopBlocks）。discord.ts 傳入 hints，agent-loop 合併產出完整 breakdown。修正 session-note 遺漏加入 agentLoopBlocks | message-trace.ts, agent-loop.ts, discord.ts |
| 2026-04-05 | **Sprint 8: Trace 管線審計 + _AIDocs 全面更新** — ①message-trace.ts 新增 TracePromptAssembly / TraceProviderSelection 型別 + recordPromptAssembly / recordProviderSelection / appendAgentLoopBlocks 方法②agent-loop.ts memory recall 路徑補 trace③discord.ts 接線 prompt assembly trace + provider selection trace④prompt-assembler.ts 新增 AssembleTraceOutput 追蹤模組注入/跳過⑤_INDEX.md 全面重寫（專案簡介+架構摘要+核心子系統表+文件清單）⑥新�� 12 份模組文件（agent-loop/platform/context-engine/prompt-assembler/dashboard/memory-engine/tool-registry/accounts/event-bus/hooks/workflow/safety）⑦更新 6 份過時文件 | message-trace.ts, agent-loop.ts, discord.ts, prompt-assembler.ts, _INDEX.md, modules/*.md (×18) |
| 2026-04-05 | **docs: _AIDocs 三份模組文件同步更新** — config.md 路徑更新至 src/core/config.ts + 補齊 V2 擴充欄位（provider 三層分離、memory、workflow、accounts、CE、modes、subagents、mcpServers、hooks 等）。session.md 加入新版 SessionManager（class 設計 + Turn Queue + checksum 持久化）。reply.md 加入新版 reply-handler（streaming live-edit + chunk fallback + autoThread） | modules/config.md, modules/session.md, modules/reply.md |
| 2026-04-05 | **feat: Memory Staleness Check (R14)** — context-builder 注入 atom 時掃描檔案路徑引用（`/abs/path`、`src/rel/path`），existsSync 驗證不存在 → ���記 `[⚠️ stale: ...]`。FILE_PATH_RE 匹配反引號絕對路徑 + src/ 相對路徑。overhead < 10ms（同步 IO + dedup） | context-builder.ts |
<!-- 滾動淘汰：Task UI Components v2 (2026-04-05)、prompt-assembler 接線 (2026-04-05)、/commit+/pr skills (2026-04-05)、CLI Provider (2026-04-05) -->
